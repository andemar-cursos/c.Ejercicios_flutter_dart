import 'package:flutter/material.dart';

class FirtsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Firts Page'),
      )
    );
  }
}